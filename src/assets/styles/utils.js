import styled from 'styled-components';

export const Button = styled.button`
  background-color: rgb(245, 132, 111);
  text-transform: uppercase;
  display: block;
  color: white;
  padding: 1.4rem;
  border: 0px solid rgba(0, 0, 0, 0.2);
  border-radius: 2px;
  box-shadow: 0px 0x 2px rgba(0, 0, 0, 0.2);
  cursor: pointer;
`;
